package com.Feedy.Enum;

public enum OrderStatus {
    Order_accepted,
    Food_preparing,
    Order_pending,
    Declined,
    On_the_way,
    Delivered,
    Cancel_by_restaurant,
    Cancel_by_customer,
    Cancel_by_delivery_agent
}
